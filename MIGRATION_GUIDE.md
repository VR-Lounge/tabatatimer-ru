# Руководство по миграции tabatatimer.ru на GitHub Pages

## ✅ Что уже сделано

1. ✅ Создан `.gitignore` для исключения ненужных файлов
2. ✅ Конвертирован `index.php` → `index.html` (удален PHP код)
3. ✅ PHP токены заменены на JavaScript генерацию
4. ✅ Формы обновлены для работы с EmailJS (требует настройки)
5. ✅ Создан файл `CNAME` для кастомного домена
6. ✅ Создан `_config.yml` для GitHub Pages
7. ✅ Создана инструкция по настройке EmailJS (`EMAILJS_SETUP.md`)

## 📋 Что нужно сделать вручную

### Шаг 1: Настройка EmailJS

**ВАЖНО:** Формы не будут работать до настройки EmailJS!

Следуйте инструкциям в файле `EMAILJS_SETUP.md`:
1. Зарегистрируйтесь на https://www.emailjs.com/
2. Создайте Email Service
3. Создайте Email Template
4. Получите Public Key
5. Обновите в `index.html`:
   - `YOUR_PUBLIC_KEY` → ваш Public Key
   - `YOUR_SERVICE_ID` → ваш Service ID
   - `YOUR_TEMPLATE_ID` → ваш Template ID

### Шаг 2: Инициализация Git репозитория

```bash
cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/public_html"

# Инициализация Git
git init
git add .
git commit -m "Initial commit: Tabata Timer migration to GitHub Pages"
```

### Шаг 3: Создание GitHub репозитория

1. Перейдите на https://github.com/new
2. Создайте новый публичный репозиторий: `tabatatimer-ru`
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### Шаг 4: Подключение к GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/tabatatimer-ru.git
git branch -M main
git push -u origin main
```

### Шаг 5: Настройка GitHub Pages

1. Перейдите в Settings → Pages вашего репозитория
2. В разделе "Source":
   - Выберите "Deploy from a branch"
   - Branch: `main`
   - Folder: `/ (root)`
3. Нажмите "Save"

### Шаг 6: Проверка временного URL

После настройки GitHub Pages (обычно 1-2 минуты):
1. Откройте `https://YOUR_USERNAME.github.io/tabatatimer-ru/`
2. Проверьте, что сайт работает
3. Проверьте формы (после настройки EmailJS)

### Шаг 7: Настройка DNS в nic.ru

**ВАЖНО:** Делайте это только после проверки работы на GitHub Pages!

После переноса домена в nic.ru:

1. Войдите в панель управления nic.ru
2. Перейдите в управление DNS для домена `tabatatimer.ru`
3. Добавьте A-записи (4 штуки):
   ```
   @     A    185.199.108.153
   @     A    185.199.109.153
   @     A    185.199.110.153
   @     A    185.199.111.153
   ```
4. Добавьте CNAME-запись:
   ```
   www   CNAME   YOUR_USERNAME.github.io
   ```
5. Сохраните изменения

### Шаг 8: Ожидание распространения DNS

- DNS изменения распространяются от 5 минут до 48 часов
- Обычно занимает 1-4 часа
- Проверить можно на https://dnschecker.org/

### Шаг 9: Включение HTTPS

Через 24 часа после привязки домена:
1. Перейдите в Settings → Pages
2. Включите "Enforce HTTPS"
3. GitHub автоматически выдаст SSL сертификат

## 🔍 Проверка после миграции

### Что проверить:

1. ✅ Сайт открывается по домену `tabatatimer.ru`
2. ✅ Все изображения загружаются
3. ✅ CSS и JavaScript работают
4. ✅ Таймер работает корректно
5. ✅ Формы отправляют письма (после настройки EmailJS)
6. ✅ Медиафайлы (аудио) загружаются
7. ✅ MediaPipe Pose работает (если используется)

### Проверка путей к ресурсам

Все пути должны быть относительными:
- ✅ `href="assets/css/style.css"` (правильно)
- ❌ `href="/assets/css/style.css"` (может не работать на GitHub Pages)
- ❌ `href="https://tabatatimer.ru/assets/css/style.css"` (не нужно)

## ⚠️ Важные замечания

1. **PHP не работает на GitHub Pages** - все серверные функции заменены на клиентские решения
2. **EmailJS требует настройки** - формы не будут работать до настройки (см. `EMAILJS_SETUP.md`)
3. **.htaccess не работает** - правила редиректа и сжатия нужно настроить через GitHub Pages или в коде
4. **Файл `mailer2.php` больше не нужен** - можно удалить после проверки работы EmailJS

## 🐛 Решение проблем

### Проблема: Сайт не открывается на GitHub Pages

**Решение:**
- Проверьте, что файл `index.html` находится в корне репозитория
- Проверьте настройки GitHub Pages (Settings → Pages)
- Проверьте логи сборки в Actions

### Проблема: Изображения не загружаются

**Решение:**
- Проверьте, что пути относительные (без `/` в начале)
- Проверьте, что файлы закоммичены в Git
- Проверьте регистр букв в путях (Linux чувствителен к регистру)

### Проблема: Формы не отправляют письма

**Решение:**
- Проверьте настройку EmailJS (см. `EMAILJS_SETUP.md`)
- Проверьте консоль браузера на ошибки
- Убедитесь, что Public Key, Service ID и Template ID правильные

### Проблема: Домен не привязывается

**Решение:**
- Проверьте, что файл `CNAME` содержит только `tabatatimer.ru` (без `www`)
- Проверьте DNS записи в nic.ru
- Подождите до 48 часов для распространения DNS

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи GitHub Actions
2. Проверьте консоль браузера (F12)
3. Проверьте DNS на https://dnschecker.org/
4. См. документацию GitHub Pages: https://docs.github.com/en/pages

## 📝 Следующие шаги после миграции

1. Настроить EmailJS (обязательно!)
2. Протестировать все функции сайта
3. Настроить мониторинг (опционально)
4. Настроить резервное копирование (опционально)
5. Оптимизировать производительность (опционально)

